import 'package:flutter/material.dart';

class PayWithEsewaScreen extends StatelessWidget {
  const PayWithEsewaScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
